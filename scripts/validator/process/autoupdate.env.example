# Example config for scripts/validator/process/autoupdate_release.sh
#
# Copy to a machine-local path (recommended): /etc/alphacore/autoupdate.env
# and set permissions: chmod 600 /etc/alphacore/autoupdate.env
#
# Then run (manually or from a timer):
#   bash /path/to/repo/scripts/validator/process/autoupdate_release.sh --config /etc/alphacore/autoupdate.env

# Network alias used by launcher defaults (local|test|finney)
NETWORK=finney

# PM2 namespace (recommended to keep AlphaCore isolated from other PM2 apps)
PM2_NAMESPACE=alphacore

# Validator identity
WALLET_NAME=yourcold
WALLET_HOTKEY=yourhot
NETUID=123

# Chain selection (optional; inferred from NETWORK if omitted)
# CHAIN_ENDPOINT=wss://finney.opentensor.io:443
# PROFILE=production

# Validation API auth (one of):
GCP_CREDS_FILE=/secure/path/gcp-creds.json
# Or export GOOGLE_OAUTH_ACCESS_TOKEN via your supervisor environment and omit GCP_CREDS_FILE.

# Optional overrides
# VALIDATOR_PROCESS_NAME=validator-yourhot-finney
# VALIDATION_PROCESS_NAME=alphacore-validation-api
# VALIDATOR_VENV_DIR=/home/ubuntu/alphacore/venv
# VALIDATION_VENV_DIR=/home/ubuntu/alphacore/.venv-validation-api
# VALIDATOR_ENV_FILE=/home/ubuntu/alphacore/env/finney/validator-yourcold-yourhot.env
# VALIDATION_ENV_FILE=/home/ubuntu/alphacore/env/finney/validation-api.env
# VALIDATION_API_ENDPOINT=http://127.0.0.1:8888
# VALIDATOR_HTTP_PORT=8000
# VALIDATION_HTTP_HOST=127.0.0.1
# VALIDATION_HTTP_PORT=8888
# WALLET_PATH=/home/ubuntu/.bittensor/wallets

# Gate file written by validator while a round is active
VALIDATOR_ROUND_LOCKFILE=/tmp/alphacore-validator-round.lock

# Optional passthrough args (examples)
# VALIDATOR_EXTRA_ARGS="--timed --tick-seconds 30"
# VALIDATION_API_EXTRA_ARGS=""
